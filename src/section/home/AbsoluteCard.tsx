const AbsoluteCard = () => {
  return (

  );
};

export default AbsoluteCard;

//className="flex flex-col items-center justify-center pr-4 border-r border-white/20 md:last:border-r-0 "
// <div className="flex flex-col items-center justify-center pr-4 border-r border-white/20 md:last:border-r-0 ">
//   <div className="flex items-center gap-1 max-w-10">
//     <span className="text-2xl font-bold text-white md:text-4xl">
//       {value}
//     </span>

//     {typeof icon === "string" ? (
//       <img src={icon} alt={label} className="h-8 w-7" />
//     ) : (
//       // Ensure we are correctly using the React component
//       React.createElement(icon, { className: "text-white text-3xl" })
//     )}
//   </div>
//   <p className="text-base text-white">{label}</p>
// </div>;
